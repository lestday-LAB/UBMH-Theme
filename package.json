{
  "name": "ubmh-theme",
  "description": "UBMH (Wikidot Underground Black Market) site theme stylesheets (build process)",
  "version": "1.0.0",
  "license": "CC-BY-SA-3.0",
  "private": true,
  "devDependencies": {
    "minify": "^9.2.0",
    "prettier": "^2.8.7",
    "stylelint": "^15.10.1",
    "stylelint-config-standard": "^32.0.0"
  },
  "scripts": {
    "build": "./build.sh",
    "clean": "rm -rf dist",
    "minify": "minify css/ubmh-impact.css > dist/css/ubmh-impact.min.css && minify css/ubmh-sandbox.css > dist/css/ubmh-sandbox.min.css",
    "stylelint": "stylelint css/*.css",
    "stylelint:fix": "stylelint --fix css/*.css",
    "prettier": "prettier -c css/*.css",
    "prettier:fix": "prettier -w css/*.css"
  }
}
